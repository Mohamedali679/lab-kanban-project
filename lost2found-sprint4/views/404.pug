extends layout

block content
  section.auth-section
    .auth-card(style="text-align:center")
      .empty-icon(style="font-size:4rem") 🔍
      h1(style="font-size:1.5rem;margin:1rem 0") Page Not Found
      p(style="color:#888;margin-bottom:1.5rem") The page you're looking for doesn't exist.
      a.submit-btn(href="/") ← Go Home
